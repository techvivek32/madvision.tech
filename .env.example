# Copy to .env.local and fill in the real values.
# For production, add the same variables in Vercel -> Project Settings -> Environment Variables.
SMTP_HOST=smtp.titan.email
SMTP_PORT=465
SMTP_USER=info@madvision.tech
SMTP_PASS=your-titan-email-password
CONTACT_EMAIL=info@madvision.tech
ADMIN_PASS=choose-a-strong-admin-passcode
